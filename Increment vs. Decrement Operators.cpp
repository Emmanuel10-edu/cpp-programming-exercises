#include <iostream>
using namespace std;

int main() {
    int count = 5;
    
    count++; // count is now 6
    cout << "After increment: " << count << endl;
    
    count--; // count goes back to 5
    cout << "After decrement: " << count << endl;
    
    return 0;
}